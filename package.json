{
  "dependencies": {
    "concurrently": "^9.0.1",
    "date-fns": "^4.1.0",
    "moment": "^2.30.1",
    "reactstrap": "^9.2.3",
    "xml2js": "^0.6.2"
  },
  "name": "project-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd express_smart-leave && npm start",
    "client": "cd react_smart-leave && npm start"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
